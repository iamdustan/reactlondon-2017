/* @flow */

import React from 'react';
import {
  FullScreen,
} from '../components';
import {colors} from '../components/theme';

export default () => <FullScreen background={colors.black} style={{zIndex: 1111}} />

